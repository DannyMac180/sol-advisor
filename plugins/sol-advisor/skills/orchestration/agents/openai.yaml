interface:
  display_name: "Sol Advisor Orchestration"
  short_description: "Route schema-v2 roles with challenge-first proof, then verify and accept"
  default_prompt: "Use $routing and challenge-first resolve_route for the exact schema-v2 role; verify and accept. Compatibility requires request."

interface:
  display_name: "Sol Advisor Orchestration"
  short_description: "Use capability-gated DeepSeek or Terra and fresh Sol review, or explicitly opt into Luna tasks"
  default_prompt: "Use $orchestration for capability-gated DeepSeek or Terra implementation and fresh Sol review; report any permitted pre-work fallback. Use the user-visible Luna task lane only when I explicitly authorize it, then use list_projects, list_threads, create_thread, wait_threads, read_thread, and send_message_to_thread to monitor, review, correct, and accept the task yourself."

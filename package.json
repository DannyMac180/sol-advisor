{
  "name": "sol-advisor-validation",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "bun tools/validate.ts validate",
    "release": "bun tools/validate.ts release",
    "release:check": "bun tools/validate.ts release-check",
    "ci": "sh plugins/sol-advisor/scripts/verify.sh && bun run test && bun run validate && bun run release:check",
    "tag:check": "bun tools/validate.ts tag-check",
    "cursor:local": "bun tools/cursor-local.ts",
    "test": "bun test plugins/sol-advisor/mcp/server.test.ts tools/cursor-local.test.ts"
  },
  "devDependencies": {
    "@types/bun": "1.3.5"
  }
}
